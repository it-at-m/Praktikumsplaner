id: initial_realm
author: MrSebastian
changes:
  - addRealm:
      name: ${SSO_REALM}

  - updateRealm:
      id: ${SSO_REALM}
      displayName: Local Realm

  - addGroup:
      realm: ${SSO_REALM}
      name: group1

  - addClientScope:
      realm: ${SSO_REALM}
      name: openid