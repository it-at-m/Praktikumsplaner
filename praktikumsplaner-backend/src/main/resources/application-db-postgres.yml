spring:
  datasource:
    url: ${app.db.postgres.url}
    username: ${app.db.postgres.username}
    password: ${app.db.postgres.password}
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
  flyway:
    locations: classpath:db/migration/postgres